/* LCM type definition class file
 * This file was automatically generated by lcm-gen
 * DO NOT MODIFY BY HAND!!!!
 */

package perllcm;
 
import java.io.*;
import java.nio.*;
import java.util.*;
import lcm.lcm.*;
 
public final class point3d_list_t implements lcm.lcm.LCMEncodable
{
    public long id;
    public int npoints;
    public perllcm.point3d_float_t points[];
    public int ncolors;
    public perllcm.color_float_t colors[];
    public long pointids[];
 
    public point3d_list_t()
    {
    }
 
    public static final long LCM_FINGERPRINT;
    public static final long LCM_FINGERPRINT_BASE = 0x48f413bcdffd07aeL;
 
    static {
        LCM_FINGERPRINT = _hashRecursive(new ArrayList<Class>());
    }
 
    public static long _hashRecursive(ArrayList<Class> classes)
    {
        if (classes.contains(perllcm.point3d_list_t.class))
            return 0L;
 
        classes.add(perllcm.point3d_list_t.class);
        long hash = LCM_FINGERPRINT_BASE
             + perllcm.point3d_float_t._hashRecursive(classes)
             + perllcm.color_float_t._hashRecursive(classes)
            ;
        classes.remove(classes.size() - 1);
        return (hash<<1) + ((hash>>63)&1);
    }
 
    public void encode(DataOutput outs) throws IOException
    {
        outs.writeLong(LCM_FINGERPRINT);
        _encodeRecursive(outs);
    }
 
    public void _encodeRecursive(DataOutput outs) throws IOException
    {
        outs.writeLong(this.id); 
 
        outs.writeInt(this.npoints); 
 
        for (int a = 0; a < this.npoints; a++) {
            this.points[a]._encodeRecursive(outs); 
        }
 
        outs.writeInt(this.ncolors); 
 
        for (int a = 0; a < this.ncolors; a++) {
            this.colors[a]._encodeRecursive(outs); 
        }
 
        for (int a = 0; a < this.ncolors; a++) {
            outs.writeLong(this.pointids[a]); 
        }
 
    }
 
    public point3d_list_t(byte[] data) throws IOException
    {
        this(new LCMDataInputStream(data));
    }
 
    public point3d_list_t(DataInput ins) throws IOException
    {
        if (ins.readLong() != LCM_FINGERPRINT)
            throw new IOException("LCM Decode error: bad fingerprint");
 
        _decodeRecursive(ins);
    }
 
    public static perllcm.point3d_list_t _decodeRecursiveFactory(DataInput ins) throws IOException
    {
        perllcm.point3d_list_t o = new perllcm.point3d_list_t();
        o._decodeRecursive(ins);
        return o;
    }
 
    public void _decodeRecursive(DataInput ins) throws IOException
    {
        this.id = ins.readLong();
 
        this.npoints = ins.readInt();
 
        this.points = new perllcm.point3d_float_t[(int) npoints];
        for (int a = 0; a < this.npoints; a++) {
            this.points[a] = perllcm.point3d_float_t._decodeRecursiveFactory(ins);
        }
 
        this.ncolors = ins.readInt();
 
        this.colors = new perllcm.color_float_t[(int) ncolors];
        for (int a = 0; a < this.ncolors; a++) {
            this.colors[a] = perllcm.color_float_t._decodeRecursiveFactory(ins);
        }
 
        this.pointids = new long[(int) ncolors];
        for (int a = 0; a < this.ncolors; a++) {
            this.pointids[a] = ins.readLong();
        }
 
    }
 
    public perllcm.point3d_list_t copy()
    {
        perllcm.point3d_list_t outobj = new perllcm.point3d_list_t();
        outobj.id = this.id;
 
        outobj.npoints = this.npoints;
 
        outobj.points = new perllcm.point3d_float_t[(int) npoints];
        for (int a = 0; a < this.npoints; a++) {
            outobj.points[a] = this.points[a].copy();
        }
 
        outobj.ncolors = this.ncolors;
 
        outobj.colors = new perllcm.color_float_t[(int) ncolors];
        for (int a = 0; a < this.ncolors; a++) {
            outobj.colors[a] = this.colors[a].copy();
        }
 
        outobj.pointids = new long[(int) ncolors];
        if (this.ncolors > 0)
            System.arraycopy(this.pointids, 0, outobj.pointids, 0, this.ncolors); 
        return outobj;
    }
 
}

